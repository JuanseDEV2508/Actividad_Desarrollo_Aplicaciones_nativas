<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal de la app -->
    <style name="Theme.PixelStore_AN" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_gamer</item>

        <item name="android:windowBackground">@color/splash_background_color</item>
    </style>

    <!-- Tema especial solo para el Splash -->
    <style name="Theme.PixelStore_AN.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
